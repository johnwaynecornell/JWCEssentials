## Repo Example
20260516_133620 This is a current artifact and works on a configured machine. The branch is temporary
```mkdir Example.NewAge
mkdir Example.NewAge
cd Example.NewAge
git clone https://github.com/johnwaynecornell/JWCEssentials
cd JWCEssentials
git switch scratch/integration-refactor
cd ..
JWCEssentials/configure.sh --newage "$(pwd)"

./in_this_context.sh Debug -- newage_get.sh JWCCommandSpawn https://github.com/johnwaynecornell/JWCCommandSpawn scratch/integration-refactor
./in_this_context.sh Debug -- newage_get_deps.sh
./in_this_context.sh Debug -- newage_all_configure.sh
./in_this_context.sh Debug -- newage_all_build_coordinated.sh Debug

#./in_this_context.sh Debug -- bash
```